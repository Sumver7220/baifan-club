---
phase: 01-vite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - postcss.config.js
  - .gitignore
autonomous: true
requirements:
  - BUILD-01
must_haves:
  truths:
    - "Developer can run npm run dev and get a Vite dev server with HMR."
    - "Developer can run npm run build and get a dist output with hashed assets."
  artifacts:
    - path: "package.json"
      provides: "Vite/Sass build scripts and dev dependencies"
    - path: "vite.config.js"
      provides: "Build and dev server configuration"
    - path: "postcss.config.js"
      provides: "Autoprefixer pipeline"
    - path: ".gitignore"
      provides: "Ignore generated artifacts"
  key_links:
    - from: "package.json scripts"
      to: "vite.config.js"
      via: "npm run dev/build"
      pattern: "vite|vite build"
    - from: "postcss.config.js"
      to: "Vite CSS pipeline"
      via: "autoprefixer"
      pattern: "autoprefixer"
---

<objective>
建立可重現的 Vite 建構流程，讓專案脫離 VS Code Live Sass 擴充依賴，並可用命令列完成開發/建構。

Purpose: 先完成 BUILD-01 的基礎設施，讓後續 SCSS 模組化與行為重構都可在同一條 pipeline 驗證。
Output: 可執行的 npm scripts 與 Vite/PostCSS 設定檔。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-vite/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 Vite 與 PostCSS 建構骨架</name>
  <files>package.json, vite.config.js, postcss.config.js</files>
  <action>新增 npm scripts（dev/build/preview）與 devDependencies（vite、sass、postcss、autoprefixer），建立 ESM 版 vite.config.js 與 postcss.config.js。Vite 設定要以 index.html 為入口、輸出 dist，符合 D-01 之前置建構需求，且不可保留對 Live Sass extension 的依賴。</action>
  <verify>
    <automated>npm install ; npm run build</automated>
  </verify>
  <done>`npm run build` 可成功輸出 dist，且 package scripts 可直接啟動開發與預覽。</done>
</task>

<task type="auto">
  <name>Task 2: 補齊建構輸出忽略規則</name>
  <files>.gitignore</files>
  <action>新增或更新 .gitignore，至少包含 node_modules 與 dist，避免建構輸出污染版本控制。若檔案已存在則保留既有規則，僅追加缺少項目。</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.gitignore','utf8');if(!/^node_modules$/m.test(s)||!/^dist$/m.test(s))process.exit(1);"</automated>
  </verify>
  <done>.gitignore 包含 node_modules 與 dist，且未破壞既有忽略規則。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine -> npm registry | 安裝套件時從外部來源拉取相依套件 |
| source config -> build output | 設定檔誤設可能導致錯誤輸出或資源遺失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | package.json | mitigate | 只加入本 phase 所需相依套件與 scripts，避免不必要執行腳本。 |
| T-01-02 | D | vite.config.js | mitigate | 設定明確輸出目錄與入口，並用 npm run build 驗證建構可重現。 |
| T-01-03 | I | .gitignore | mitigate | 忽略 dist 與 node_modules，避免敏感或冗餘檔進版控。 |
</threat_model>

<verification>
- `npm run dev` 可啟動開發伺服器。
- `npm run build` 可輸出 dist。
</verification>

<success_criteria>
- BUILD-01 達成：專案可用 Vite 命令列重現開發與建構流程。
</success_criteria>

<output>
After completion, create `.planning/phases/01-vite/01-vite-01-SUMMARY.md`
</output>

