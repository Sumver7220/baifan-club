---
phase: 01-vite
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - css/main.scss
  - css/shared/_variables.scss
  - js/main.js
  - index.html
autonomous: true
requirements:
  - BUILD-02
must_haves:
  truths:
    - "Styles are loaded through one Vite-managed SCSS entry instead of many static CSS links."
    - "SCSS shared constants exist in a dedicated module and can be reused with @use."
  artifacts:
    - path: "css/main.scss"
      provides: "Single SCSS entrypoint with @use imports"
    - path: "css/shared/_variables.scss"
      provides: "Shared compile-time constants for SCSS modules"
    - path: "js/main.js"
      provides: "Runtime entry importing SCSS and nav logic"
    - path: "index.html"
      provides: "Module script entry replacing multi-link CSS loading"
  key_links:
    - from: "index.html"
      to: "js/main.js"
      via: "<script type=module>"
      pattern: "type=\"module\""
    - from: "js/main.js"
      to: "css/main.scss"
      via: "import statement"
      pattern: "import '../css/main.scss'|import \"../css/main.scss\""
---

<objective>
建立 SCSS 模組化入口並切換 HTML 載入方式，完成 BUILD-02 所需的 `@use` 方向與單一樣式輸出路徑。

Purpose: 讓所有樣式透過 Vite 管線建構，避免多個編譯後 CSS 檔手動維護。
Output: main.scss、shared variables、main.js 與更新後的 index.html。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-vite/01-CONTEXT.md
@.planning/research/STACK.md

<interfaces>
From index.html:
- Existing static styles are currently loaded by multiple `<link rel="stylesheet" href="css/*.css">` tags.
- Existing runtime script is `<script src="js/nav.js"></script>`.

From js/nav.js:
- Side-effect script with no module exports; safe to import from entry file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立 SCSS 模組入口與共用變數骨架</name>
  <files>css/main.scss, css/shared/_variables.scss</files>
  <action>依 D-01、D-03、D-04 建立 `css/main.scss` 作為單一入口，使用 `@use` 匯入 shared 變數與既有 page scss；建立 `css/shared/_variables.scss` 放 compile-time 常數骨架。runtime CSS custom properties 保留在 base.scss，不可搬移。</action>
  <verify>
    <automated>node -e "const fs=require('fs');const a=fs.readFileSync('css/main.scss','utf8');const b=fs.readFileSync('css/shared/_variables.scss','utf8');if(!a.includes('@use')||!b.includes('$bp-mobile')||!b.includes('$bp-tablet')||!b.includes('$bp-4k'))process.exit(1);"</automated>
  </verify>
  <done>main.scss 使用 @use 串接樣式；shared variables 檔存在且可供後續 phase 擴充。</done>
</task>

<task type="auto">
  <name>Task 2: 切換 HTML/JS 入口至 Vite 模組載入</name>
  <files>js/main.js, index.html</files>
  <action>依 D-02 在 `js/main.js` 頂端 import `../css/main.scss` 並載入 `./nav.js`；`index.html` 移除多個 css link 與舊 script，改為 `<script type="module" src="/js/main.js"></script>`。保留頁面結構與語義標記，不改內容層。</action>
  <verify>
    <automated>npm run build ; node -e "const fs=require('fs');const s=fs.readFileSync('index.html','utf8');if(!s.includes('<script type=\"module\" src=\"/js/main.js\"></script>'))process.exit(1);"</automated>
  </verify>
  <done>index.html 僅使用 module script 入口，build 後樣式仍可正確載入。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTML shell -> module runtime | 入口切換失誤會造成白屏或樣式遺失 |
| SCSS module graph -> compiled CSS | 模組順序錯誤可能造成覆蓋順序回歸 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | D | index.html | mitigate | 只調整載入入口，不改 section 結構；以 npm run build 驗證輸出。 |
| T-01-05 | T | css/main.scss | mitigate | 使用 @use 與固定匯入順序（base/pages/nav/page-*），避免重複匯入。 |
| T-01-06 | E | js/main.js | accept | 僅做樣式與既有 nav 腳本載入，不引入新權限或外部呼叫。 |
</threat_model>

<verification>
- `npm run build` 通過且 dist 可載入樣式。
- `npm run dev` 下修改任一 scss 可觸發 HMR。
</verification>

<success_criteria>
- BUILD-02 達成：SCSS 入口整合完成，`@use` 模組系統可運作。
</success_criteria>

<output>
After completion, create `.planning/phases/01-vite/01-02-SUMMARY.md`
</output>


