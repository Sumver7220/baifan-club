---
phase: 01-vite
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - js/nav.js
  - index.html
autonomous: true
requirements:
  - BUILD-03
  - BUILD-04
must_haves:
  truths:
    - "Page bounds are derived from actual .page count, with no hardcoded 8/9 in navigation logic."
    - "Clerk modal opens/closes through delegated listeners without inline onclick or globalThis clerk handlers."
  artifacts:
    - path: "js/nav.js"
      provides: "Dynamic page count injection and delegated clerk modal handling"
    - path: "index.html"
      provides: "Clerk modal markup without inline onclick"
  key_links:
    - from: "js/nav.js"
      to: "document.documentElement.style"
      via: "setProperty('--page-count', count)"
      pattern: "--page-count"
    - from: "js/nav.js"
      to: "#clerkModal"
      via: "event delegation click handler"
      pattern: "clerk-modal|data-clerk-id"
---

<objective>
移除導航硬編碼頁數並完成 Clerk Modal delegated event listeners 遷移，落實 BUILD-03 與 BUILD-04。

Purpose: 提升可維護性，避免新增/刪除頁面時破版，並消除 Clerk modal 的 inline/global handler。
Output: 更新後 nav.js 與無 inline clerk onclick 的 index.html。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-vite/01-CONTEXT.md

<interfaces>
From js/nav.js (current):
- `goToPage(index)` updates track transform and nav active state.
- Hardcoded max page logic currently uses literal `8` in swipe bounds and next-page checks.
- Clerk modal currently uses `globalThis.openClerkModal` and `globalThis.closeClerkModal`.

From index.html (current):
- Clerk modal uses inline `onclick="closeClerkModal()"` on overlay and close button.
- Clerk cards expose `data-clerk-id` and `data-clerk-name` for modal payload.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 導航頁數改為 DOM 動態推導</name>
  <files>js/nav.js</files>
  <behavior>
    - Test 1: 啟動後會以 `.page` 數量覆蓋 `--page-count`。
    - Test 2: swipe 邊界與 next/prev 判斷使用動態總頁數，不再依賴硬編碼 8。
  </behavior>
  <action>依 D-06、D-07，於初始化時計算 `document.querySelectorAll('.page').length`，注入 `--page-count` 並以該值推導最大 index 與最大 offset。保留 `base.scss` fallback `--page-count: 9`。不可保留任何 page-bound hardcode literal。</action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('js/nav.js','utf8');const hasCount=s.includes('querySelectorAll(\".page\")')||s.includes(\"querySelectorAll('.page')\");const hasHard=s.includes(' < 8')||s.includes('8 * window.innerWidth')||s.includes('maxOffset = 8');if(!hasCount||hasHard)process.exit(1);"</automated>
  </verify>
  <done>頁面上限完全由 DOM 推導，新增/刪除 .page 時無需修改 JS 常數。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clerk Modal 改為 delegated listeners 並移除 inline handler</name>
  <files>js/nav.js, index.html</files>
  <behavior>
    - Test 1: 點擊 `.clerk-card[data-clerk-id]` 可開啟 modal 並填入對應資訊。
    - Test 2: 點擊 overlay、close 按鈕或 Esc 可關閉 modal。
    - Test 3: 不再有 `globalThis.openClerkModal/closeClerkModal` 與 clerk inline onclick。
  </behavior>
  <action>依 D-08（且遵守 D-09），將 Clerk modal 開關改成 document 層事件委派（click/keydown），移除 `globalThis` clerk handlers 與 `index.html` 的 clerk inline `onclick`。Menu modal 維持現狀，不做範圍外重構。</action>
  <verify>
    <automated>npm run build ; node -e "const fs=require('fs');const a=fs.readFileSync('js/nav.js','utf8');const b=fs.readFileSync('index.html','utf8');if(/globalThis\.openClerkModal|globalThis\.closeClerkModal/.test(a))process.exit(1);if(/onclick=\"closeClerkModal\(\)\"/.test(b))process.exit(1);"</automated>
  </verify>
  <done>Clerk modal 互動改由事件委派驅動，BUILD-04 驗收條件達成，且未波及 menu modal。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted DOM events -> modal state | 任意點擊事件可能誤觸 modal 流程 |
| page count from DOM -> navigation bounds | DOM 讀值錯誤會造成越界或卡頁 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | js/nav.js event delegation | mitigate | 僅對符合 selector 的目標觸發 modal 開啟/關閉，其他事件直接忽略。 |
| T-01-08 | D | js/nav.js page bounds | mitigate | 以動態總頁數計算 max index 與 offset，並用 Math.min/Math.max 夾限。 |
| T-01-09 | R | index.html modal controls | mitigate | 移除 inline onclick，集中於單一 JS 事件層，降低不可追蹤入口。 |
</threat_model>

<verification>
- `npm run build` 通過。
- 全站搜尋不再出現 Clerk inline onclick 與 Clerk globalThis handler。
</verification>

<success_criteria>
- BUILD-03、BUILD-04 達成：頁數動態推導與 Clerk modal delegated listeners 完成。
</success_criteria>

<output>
After completion, create `.planning/phases/01-vite/01-vite-03-SUMMARY.md`
</output>


